# Agent榜单中心 v1.0.0 Goal 启动文本

```text
/goal 在 2026-07-31 前按照 D:\AIGC\MoviePilot\MoviePilot-Plugins\plugins.v2\agentrank\ai_spec\v1.0.0_implementation_plan.md 完成 Agent榜单中心 v1.0.0，并持续维护 D:\AIGC\MoviePilot\MoviePilot-Plugins\plugins.v2\agentrank\ai_spec\v1.0.0_progress.json。

每回合先读进度账本与当前工作单元，再运行 git status、git log -15 和计划中的 baseline smoke。只处理当前工作单元；验收通过后仅更新账本允许字段，提交该单元并记录 commit，随后自动进入下一任务或阶段。验收失败不得提交。始终保留且不得暂存 tests/static/test_localtoolkit_standard_completion.py 的既有用户改动。不得擅自改写计划定义，不得 amend、rebase、reset、push、merge 或发布。只有缺少产品决策、无关改动无法隔离、需要 push、需要合入 main 或需要正式发布时暂停等待用户确认；确认后从账本继续。发布完成条件以计划的 Phase 8 远端与 MP 本地证据为准。
```
